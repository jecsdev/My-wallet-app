package com.jecsdev.mywalletapp.presentation.uihelpers

/**
 * This is an enum that indicates the type of keyboard to show in each situation.
 */
enum class InputType {
    TEXT,
    ASCII,
    NUMBER,
    PHONE,
    URI,
    EMAIL,
    PASSWORD,
    NUMBER_PASSWORD,
    DECIMAL
}